---
const columns = [
  {
    title: "Frontend",
    items: ["TypeScript", "Vue / React", "Astro", "Tailwind CSS", "Animation Design"],
  },
  {
    title: "3D & Interaction",
    items: ["Three.js", "WebGL", "Shader Basics", "Micro-interactions", "Visual Prototyping"],
  },
  {
    title: "Engineering",
    items: ["Node.js", "API Integration", "Performance Tuning", "Git Workflow", "Deployment"],
  },
];
---

<section id="skills" class="section bg-alt">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-14 reveal">
      <span class="badge" data-i18n="skills.badge">Skills</span>
      <h2 class="section-title mt-4" data-i18n="skills.title">技术栈与能力结构</h2>
      <p class="section-subtitle mx-auto" data-i18n="skills.subtitle">以完整交付为导向，覆盖产品体验、前端工程和可视化实现。</p>
    </div>

    <div class="grid md:grid-cols-3 gap-6">
      {columns.map((col) => (
        <article class="card p-6 reveal">
          <h3 class="text-xl font-semibold">{col.title}</h3>
          <ul class="mt-5 space-y-3">
            {col.items.map((item) => (
              <li class="flex items-center gap-2 text-sm text-secondary">
                <span class="w-1.5 h-1.5 rounded-full bg-[var(--accent)]"></span>
                <span>{item}</span>
              </li>
            ))}
          </ul>
        </article>
      ))}
    </div>

    <div class="card p-6 md:p-7 mt-7 reveal">
      <p class="text-sm text-secondary" data-i18n="skills.workingStyle">Working Style</p>
      <div class="mt-3 flex flex-wrap gap-2">
        <span class="px-3 py-1.5 rounded-full bg-alt border border-primary text-xs text-secondary" data-i18n="skills.tag1">Design-aware Development</span>
        <span class="px-3 py-1.5 rounded-full bg-alt border border-primary text-xs text-secondary" data-i18n="skills.tag2">Component Thinking</span>
        <span class="px-3 py-1.5 rounded-full bg-alt border border-primary text-xs text-secondary" data-i18n="skills.tag3">Performance First</span>
        <span class="px-3 py-1.5 rounded-full bg-alt border border-primary text-xs text-secondary" data-i18n="skills.tag4">Simple Architecture</span>
        <span class="px-3 py-1.5 rounded-full bg-alt border border-primary text-xs text-secondary" data-i18n="skills.tag5">Long-term Maintainability</span>
      </div>
    </div>
  </div>
</section>
